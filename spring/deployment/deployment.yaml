apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring
  namespace: twin-spring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring
  template:
    metadata:
      labels:
        app: spring
    spec:
      containers:
      - name: spring
        image: yeongjin95/twin-spring:77
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: spring-config
        env:
          - name: OPENWEATHER_API_KEY
            valueFrom:
              secretKeyRef:
                name: spring-credentials
                key: weather-key
          - name: SPRING_DATASOURCE_USERNAME
            valueFrom:
              secretKeyRef:
                name: spring-credentials
                key: db-user
          - name: SPRING_DATASOURCE_PASSWORD
            valueFrom:
              secretKeyRef:
                name: spring-credentials
                key: db-password
        resources:
          requests:
            cpu: 500m
            memory: 512Mi
          limits:
            cpu: "1"
            memory: 1Gi
        readinessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 20
